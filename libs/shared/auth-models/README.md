# auth-models

This library contains shared authentication types and models for AssetSim Pro.

## Building

Run `nx build auth-models` to build the library.

## Running unit tests

Run `nx test auth-models` to execute the unit tests via [Vitest](https://vitest.dev/).

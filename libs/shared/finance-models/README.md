# finance-models

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build finance-models` to build the library.

## Running unit tests

Run `nx test finance-models` to execute the unit tests via [Vitest](https://vitest.dev/).

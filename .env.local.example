# Local Development Environment Configuration
# This file is a template for creating your .env.local file
# Copy this file to .env.local and adjust values as needed
# DO NOT commit .env.local to version control - it's for local development only

# SQL Server Configuration
SQL_CONNECTION_STRING=Server=localhost,1433;Database=AssetSimPro;User Id=sa;Password=LocalDevPassword123!;Encrypt=true;TrustServerCertificate=true

# Redis Configuration
REDIS_CONNECTION_STRING=localhost:6379

# Azure Storage (Azurite) Configuration
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://localhost:10000/devstoreaccount1;QueueEndpoint=http://localhost:10001/devstoreaccount1;TableEndpoint=http://localhost:10002/devstoreaccount1;

# Azure SignalR Service (Emulator) Configuration
# The emulator container is exposed on port 8888 (docker-compose.yml line 56).
# Note: The Azure SignalR Local Emulator specifies Port=8999 in its connection string format.
# This is the emulator's standard output format, not an error. Clients connect to localhost:8888,
# but the connection string uses Port=8999 as part of the emulator's internal configuration.
AZURE_SIGNALR_CONNECTION_STRING=Endpoint=http://localhost;Port=8999;AccessKey=ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789ABCDEFGH;Version=1.0;

# Note: This file is primarily for non-Azure Functions configuration.
# Azure Functions configuration should be in backend/local.settings.json (not here).
# The values below are included for reference only.

# Application Configuration
NODE_ENV=development

# Kendo UI License Key
# Obtain your license key from https://www.telerik.com/account/
# 
# Local Development: Set this value in .env.local (this file)
# GitHub Actions CI/CD: Set as repository secret named KENDO_UI_LICENSE
# Azure DevOps CI/CD: Add to variable group 'assetsim-prod-vars' and mark as secret
# 
# If not configured, Kendo UI will run in trial mode with watermarks
KENDO_UI_LICENSE=your-kendo-license-key-here

# Local Development Environment Configuration
# This file is a template for creating your .env.local file
# Copy this file to .env.local and adjust values as needed
# DO NOT commit .env.local to version control - it's for local development only

# ============================================================================
# MOCKED LOCAL DEVELOPMENT (DEFAULT)
# ============================================================================
# By default, all services are mocked in-memory for local development:
# - SQLite (in-memory) replaces SQL Server
# - MemoryCache (in-memory) replaces Redis
# - MockSignalR (in-memory) replaces Azure SignalR
# - MockEventHub (in-memory) replaces Azure Event Hubs
#
# No additional configuration needed! Just run:
# npm run start:dev

NODE_ENV=development

# ============================================================================
# OPTIONAL: Connect to Real Azure Services
# ============================================================================
# If you need to test with real Azure services instead of mocks,
# uncomment and configure the connection strings below.
# The backend will automatically detect these and use real services.

# SQL Server Configuration (Optional - only if testing with real Azure SQL)
# SQL_CONNECTION_STRING=Server=<your-server>.database.windows.net,1433;Database=AssetSimPro;User Id=<user>;Password=<password>;Encrypt=true;TrustServerCertificate=false

# Redis Configuration (Optional - only if testing with real Azure Cache for Redis)
# REDIS_CONNECTION_STRING=<your-redis>.redis.cache.windows.net:6380,password=<password>,ssl=True

# Azure SignalR Service (Optional - only if testing with real Azure SignalR)
# AZURE_SIGNALR_CONNECTION_STRING=Endpoint=https://<your-signalr>.service.signalr.net;AccessKey=<key>;Version=1.0;

# Azure Event Hub (Optional - only if testing with real Azure Event Hubs)
# EVENT_HUB_CONNECTION_STRING=Endpoint=sb://<your-namespace>.servicebus.windows.net/;SharedAccessKeyName=<name>;SharedAccessKey=<key>


# Note: This file is primarily for non-Azure Functions configuration.
# Azure Functions configuration should be in backend/local.settings.json (not here).
# The values below are included for reference only.

# Application Configuration
NODE_ENV=development

# Kendo UI License Key
# Obtain your license key from https://www.telerik.com/account/
# 
# Local Development: Set this value in .env.local (this file)
# GitHub Actions CI/CD: Set as repository secret named KENDO_UI_LICENSE
# Azure DevOps CI/CD: Add to variable group 'assetsim-prod-vars' and mark as secret
# 
# If not configured, Kendo UI will run in trial mode with watermarks
KENDO_UI_LICENSE=your-kendo-license-key-here

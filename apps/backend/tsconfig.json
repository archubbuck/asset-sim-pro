{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "dist",
    // NOTE: rootDir is not explicitly set for TypeScript compilation (used for type checking).
    // The actual build process uses esbuild (see build.mjs) which:
    //   1. Bundles code into dist/functions/ matching Azure Functions structure
    //   2. Resolves @assetsim/* path mappings during build
    //   3. Bundles shared libraries inline (no runtime resolution needed)
    //
    // RESOLVED: Both known limitations from previous implementation have been fixed:
    //   ✓ Output Structure: esbuild outputs to dist/functions/ (not dist/apps/backend/src/functions/)
    //   ✓ Runtime Module Resolution: esbuild resolves path mappings at build time
    //
    // To build: npm run build (uses build.mjs with esbuild)
    // For type-checking only: npm run build:tsc
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "node",
    "importHelpers": false,
    "baseUrl": ".",
    // Path mappings for TypeScript type checking during development.
    // These are resolved by esbuild during the build process (see build.mjs).
    "paths": {
      "@assetsim/shared/finance-models": [
        "../../libs/shared/finance-models/src/index.ts"
      ],
      "@assetsim/shared/error-models": [
        "../../libs/shared/error-models/src/index.ts"
      ]
    }
  },
  // Include both backend source and shared library source for compilation.
  // This ensures TypeScript can resolve imports during type checking and compilation.
  "include": [
    "src/**/*",
    "../../libs/shared/error-models/src/**/*",
    "../../libs/shared/finance-models/src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.spec.ts",
    "**/*.test.ts",
    "**/__tests__/**",
    "../../libs/**/*.spec.ts",
    "../../libs/**/*.test.ts"
  ]
}

# AssetSim Pro Environment Configuration Template
# Copy this file to .env.local for local development

# ============================================================================
# KENDO UI LICENSE (REQUIRED)
# ============================================================================
# Obtain your license key from https://www.telerik.com/account/
# This is required for both local development and CI/CD builds
KENDO_UI_LICENSE=your_license_key_here

# ============================================================================
# LOCAL DEVELOPMENT (Mocked Services)
# ============================================================================
# For local development, all services are mocked in-memory:
# - SQLite (in-memory) replaces SQL Server
# - MemoryCache (in-memory) replaces Redis
# - MockSignalR (in-memory) replaces Azure SignalR
# - MockEventHub (in-memory) replaces Azure Event Hubs
#
# No connection strings needed for local development!
# Simply run: npm run start:dev
#
# The backend will automatically detect NODE_ENV=development and use mocks.

# Application Configuration
NODE_ENV=development

# ============================================================================
# PRODUCTION CONFIGURATION (Azure Cloud)
# ============================================================================
# These settings are only used in production deployment to Azure
# They are injected via Azure Key Vault and environment variables

# SQL Server Configuration (Production only)
# SQL_CONNECTION_STRING=Server=<server>.database.windows.net,1433;Database=AssetSimPro;...

# Redis Configuration (Production only)
# REDIS_CONNECTION_STRING=<redis-host>.redis.cache.windows.net:6380,...

# Azure SignalR Service (Production only)
# AZURE_SIGNALR_CONNECTION_STRING=Endpoint=https://<name>.service.signalr.net;...

# Azure Event Hub (Production only)
# EVENT_HUB_CONNECTION_STRING=Endpoint=sb://<namespace>.servicebus.windows.net/;...

# ============================================================================
# CI/CD CONFIGURATION NOTES
# ============================================================================
# GitHub Actions: Set KENDO_UI_LICENSE as a repository secret
# Azure DevOps: Add to variable group 'assetsim-prod-vars' and mark as secret
# Production: Connection strings are injected via Azure Key Vault references

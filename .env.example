# AssetSim Pro Environment Configuration Template
# Copy this file to .env for local development
# Note: Docker Compose automatically reads from .env file

# ============================================================================
# KENDO UI LICENSE (REQUIRED)
# ============================================================================
# Obtain your license key from https://www.telerik.com/account/
# This is required for both local development and CI/CD builds
KENDO_UI_LICENSE=your_license_key_here

# ============================================================================
# LOCAL DEVELOPMENT (Host-based - legacy)
# ============================================================================
# These settings are for running the app directly on your host machine
# without containers. Use these if running `npm start` directly.

# SQL Server Configuration (localhost for host-based development)
SQL_CONNECTION_STRING=Server=localhost,1433;Database=AssetSimPro;User Id=sa;Password=LocalDevPassword123!;Encrypt=true;TrustServerCertificate=true

# Redis Configuration
REDIS_CONNECTION_STRING=localhost:6379

# Azure Storage (Azurite)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://localhost:10000/devstoreaccount1;QueueEndpoint=http://localhost:10001/devstoreaccount1;TableEndpoint=http://localhost:10002/devstoreaccount1;

# Azure SignalR Service (Emulator)
AZURE_SIGNALR_CONNECTION_STRING=Endpoint=http://localhost;Port=8999;AccessKey=ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789ABCDEFGH;Version=1.0;

# ============================================================================
# CONTAINERIZED DEVELOPMENT (Docker Compose)
# ============================================================================
# These settings are automatically used by docker-compose.yml and docker-compose.dev.yml
# Service names (sql, redis, etc.) are automatically resolved by Docker's internal DNS
# 
# NOTE: The environment variables in docker-compose files override these values
# with container-appropriate service names. You don't need to change anything here.
# 
# Container connection strings are defined in:
# - docker-compose.yml (production stack)
# - docker-compose.dev.yml (development stack with hot reload)

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
NODE_ENV=development

# ============================================================================
# CI/CD CONFIGURATION NOTES
# ============================================================================
# GitHub Actions: Set KENDO_UI_LICENSE as a repository secret
# Azure DevOps: Add to variable group 'assetsim-prod-vars' and mark as secret
# Production: Connection strings are injected via Azure Key Vault references
